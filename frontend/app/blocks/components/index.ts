/**
 * Block Components — Public API
 *
 * Re-exports all block components and the central BlockRenderer
 * for use in bio-layout.tsx and other consumers.
 */
export { BlockRenderer } from './BlockRenderer';
export type { BlockRendererProps } from './BlockRenderer';
export type { BlockComponentProps, BlockWrapperProps } from './types';
